# name: Terraform CI/CD
# on:
#   push:
#     branches:
#       - main
#       - dev
#     tags:
#       - 'v*'
#   pull_request:
#     branches:
#       - main
#       - dev
# jobs:
#   terraform:
#     runs-on: ubuntu-latest
#     strategy:
#       matrix:
#         env: [dev, prd]
#     steps:
#       - uses: actions/checkout@v3
#       - name: Setup Terraform
#         uses: hashicorp/setup-terraform@v2
#       - name: Init Terraform
#         run: terraform init -backend-config=terraform/backends/${{ matrix.env }}.config
#       - name: Terraform Plan
#         run: terraform plan -var-file=terraform/${{ matrix.env }}.tfvars
#       - name: Terraform Apply (on tag only)
#         if: startsWith(github.ref, 'refs/tags/')
#         run: terraform apply -auto-approve -var-file=terraform/${{ matrix.env }}.tfvars
